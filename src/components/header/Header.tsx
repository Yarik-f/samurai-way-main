import React from 'react';
import header from "../../icon-header.svg";

export const Header = () => {
    return (
        <header className={'header'}>
            <img src={header} alt=""/>
        </header>
    );
};
